# Project Memory — Louis Lock Real Estate site

Facts and decisions specific to this repo. Not personal/career info — see [MEMORY.md](MEMORY.md) for that.

## Site structure
- Static HTML site, hand-authored (index, about, residential, commercial, contact, resources, blog, testimonials, partners).
- Blog currently covers Seattle neighborhoods: Beacon Hill, Magnolia, Mt Baker, Seward Park.
- Served locally via `.claude/serve.ps1` + `.claude/launch.json`.
- Main nav is a "Resources" hover dropdown (About, Journal, Testimonials, Partners nested under it) plus top-level Residential/Commercial/Contact, added 2026-07-10 to reduce nav clutter as pages grew.
- `testimonials.html` exists and has real content but is intentionally unlisted (no nav/footer links anywhere, `noindex` meta tag) until Louis finishes gathering remaining photos/quotes — see Decisions log. Reachable only by direct URL.
- `partners.html` is a placeholder "under construction" page, linked from the Resources dropdown, waiting on real partner specifics from Louis.

## Brand voice
- Tagline energy: "Keep Seattle, Seattle" — growth-minded but neighborhood-protective.
- Site emphasizes: born-and-raised-local credibility, serves first-time buyers through investors/commercial/development clients.
- Apply the writing-style rules in [RULES.md](RULES.md) to any blog/listing copy generated for this site.

## Business direction (relevant to site content)
- Early-stage push toward development advisory: helping clients start developing their own properties and make money on it, not just brokering sales. This lines up with the existing "Development" flow-node and the commercial page's "acquisition, leasing, and development" advisory language — it's a real, current direction, not aspirational filler.
- If/when blog posts, service pages, or CTAs get written around development, lean into this: practical, client-can-do-this framing, not "Louis the developer" framing (that's a later stage).
- Apply the client-framing rule in [RULES.md](RULES.md) here specifically: development content should read as "here's how we build your roadmap together," not "here's what Louis can build."

## Contact info (public, on contact.html)
- Phone: (206) 618-6409
- Email: inquiry@louisklock.com — live as of 2026-07-09 (Google Workspace, MX/SPF verified via DNS lookup before swapping the site over). No longer using the exprealty.com placeholder.
- Office address shown: 1455 NW Leary Way, Suite 400, Seattle, WA 98107. This is intentionally *not* Louis's home address — see privacy rule in [RULES.md](RULES.md) / [MEMORY.md](MEMORY.md).

## Known site gaps (not yet fixed)
- contact.html's office map is still explicitly placeholder/decorative ("stylized for illustration"). Not wired to a real embed yet.
- contact.html's "Office Hours" block (Mon–Fri 8–6, Sat 9–4, Sun by appointment) is wrong/outdated per Louis. He wants to hold off fixing this until he revises his actual schedule — don't update these hours until he gives the real numbers.

## Linktree
- Louis's Linktree: https://linktr.ee/thelouislock. Wired into contact.html as a real, generated QR code (`assets/qr-linktree.svg`) plus a text link — replaces the old decorative/fake QR pattern.
- QR was generated locally in-browser using the kazuhikoarase/qrcode-generator library (MIT, fetched with Louis's explicit approval — see [[feedback-external-downloads]] rule in RULES.md), error correction level H, then recolored to the site's charcoal/off-white palette. Verified structurally correct (finder patterns, module count 33x33 / type 3) but not scanned with a physical phone — worth a real scan test before considering this fully done.
- Open item: Louis wants the Linktree page itself reskinned to match this same charcoal/gold/off-white theme for visual consistency. Not started.

## Decisions log
- 2026-07-09: Fixed residential.html's "Buyer Roadmap" — it had a generic/wrong 4-step process (Discovery Call → Financing → Guided Search → Offer & Close) that didn't match Louis's real process. Replaced with the real 8-step process from his actual "Buying Seattle" client guide (Instagram carousel, `Buyers Guide (Instagram Post (45)).zip`): Consultation & Custom Roadmap → Buyer Representation Agreement → Financing & Pre-Approval → Home Search & Education → Offer Strategy & Negotiation → Inspection & Due Diligence → Escrow & Closing Preparation → Closing & Moving Day. Also added a prominent "Schedule a Consultation" button directly in both the Buyer and Seller roadmap section headers (previously the only CTA was buried at the very bottom of the page after both roadmaps). Seller Roadmap content left as-is — no reference material was provided for it, only the buyer guide.
- 2026-07-09: Built a real, gated download for the Buyer's Guide on resources.html. Compiled Louis's 7-slide Instagram carousel guide (`Buyers Guide (Instagram Post (45)).zip`, saved to `assets/buyers-guide/` and the source images) into an actual 7-page PDF (`assets/louis-lock-buyers-guide.pdf`, built via HTML→print-to-pdf since this machine has no Python/pypdf — Edge headless `--print-to-pdf` was used instead). The "Download PDF" link on the Buyer's Guide card (previously a completely dead link — true for all 6 guide cards, only Buyer's Guide has real content behind it now) now opens a name+email gate modal; on submit it POSTs to the real Formspree endpoint (lead capture) and auto-triggers the PDF download. Seller's/Commercial/Rightsizing/Investment Guide cards are still unwired — no source documents provided for those yet.
- Open item: the buyer guide also included "Things to Avoid Before Closing" and "Understanding Title & Escrow" content that isn't reflected on the site yet — could become a Resources guide entry if Louis wants it added.
- Open item: the other 5 "Download PDF" guide cards on resources.html (Seller's, Commercial, Rightsizing, Investment, ...) are still dead links with no real document behind them — same gate pattern is ready to reuse once Louis provides source content for those guides.
- 2026-07-09: index.html's homepage "About" teaser section had a generic abstract SVG silhouette instead of Louis's actual headshot — replaced with his real photo (same image used on about.html), verified at desktop and mobile widths. Separately confirmed about.html's own headshot was never actually broken — several rounds of "it's broken" turned out to be screenshot artifacts (mid-CSS-transition captures, wrong viewport width during measurement), not real bugs. Lesson: verify with a real screenshot at correct viewport before trusting automated headless captures on this site, since it uses scroll-triggered reveal animations.
- 2026-07-09: Updated contact.html with real phone/email/office address, replacing placeholder data. Used exprealty.com email initially since inquiry@louisklock.com wasn't live yet; used a real office address instead of Louis's home address, per privacy rule.
- 2026-07-09: louisklock.com email hosting (Google Workspace) confirmed live — MX records point to Google, SPF record present. Swapped contact.html from louis.lock@exprealty.com to inquiry@louisklock.com.
- 2026-07-09: Site polish pass — replaced dead `href="#"` social links (Instagram, Facebook, TikTok) with real profile URLs across all 11 pages; added `target="_blank" rel="noopener"`. Added eXp Realty brokerage disclosure ("Licensed Real Estate Broker, eXp Realty, LLC — Washington State") to the footer on all 11 pages, since the contact email implied the affiliation but no page stated it (WA broker advertising disclosure norm). Swapped the Formspree `FORM_ENDPOINT` from an unreplaced template placeholder (`mwvdnlrp` — never verified as belonging to Louis) to Louis's confirmed real form (`mdarrrad`) across all 11 pages; removed the now-stale "replace with your Formspree endpoint" comment.
- 2026-07-09: Verified full site against these memory files — no home-address leaks, office address/phone/email only appear where documented (contact.html), brokerage disclosure and social links present on all 11 pages, no leftover placeholder Formspree ID, no console/network errors on any of the 11 pages, all 3 social profile URLs return HTTP 200.
- 2026-07-09: Replaced contact.html's fake decorative "QR code" pattern with a real, generated QR linking to the Linktree, plus a text link and updated copy ("Scan to Connect" instead of the old vCard-implying "Scan to Save Our Contact," since the QR doesn't actually do a contact-card save).
- 2026-07-10: Shrunk the headshots on about.html and index.html — Louis felt they were too large/zoomed-in ("in your face"). Reduced both from ~460-520px boxes down to a fixed 290x340px, and zoomed the crop out slightly (object-position). Also moved about.html's headshot section from right after the hero to right after the "Keep Seattle, Seattle" mission statement, per Louis's request. Along the way, found and fixed a real bug: the first resize attempt used `max-width` + `margin:0 auto` on a container that only holds absolutely-positioned children (the img), which collapsed to zero width since there was no in-flow content to establish a size — fixed by using an explicit `width` instead. Verified via computed-dimension checks on both pages; full visual re-check only completed on about.html (index.html's heavier use of `vh`-based hero sections made headless-screenshot scrolling unreliable during verification, but both pages received the identical fix). **Not yet pushed to `louisklock` as of this entry** — confirm before assuming it's live.
- 2026-07-10: Full audit of all 4 `.claude/*.md` memory files against the live production site (louisklock.com) — checked privacy (no home address/personal email leaks), contact info accuracy, Formspree endpoint consistency, brokerage disclosure, social links, Linktree, buyer roadmap content, blog neighborhoods, gated PDF download (asset returns HTTP 200), and that documented "known gaps" (office map, office hours) are still correctly untouched. Everything passed. Fixed one stale fact found in MEMORY.md (still listed the retired exprealty.com email as current).
- 2026-07-10: Built `testimonials.html` (4 client stories: Don & Mary, Phuong & Victor, Rich & Joy, Federico — first-names-only per Louis's anonymity preference) and a placeholder `partners.html`. Real client photos (from `testimonialpictures.zip`) processed and saved to `assets/testimonials/` — cropped the John L. Scott "SOLD" sign out of the Phuong's-parents photo since that sale predates Louis's eXp affiliation. Rich & Joy's real quote added; Don & Mary, Phuong & Victor, and Federico still show placeholder quotes pending Louis. Restructured the main nav into a Resources hover dropdown (About/Journal/Testimonials/Partners) to cut top-level clutter as the page count grew. Per Louis's request, testimonials.html was then made "private" — pulled from all nav/footer links and given a `noindex` meta tag — until he finishes gathering remaining photos/quotes; it's unlisted, not access-controlled (still reachable at the direct URL, no login). Pushed to `louisklock` at commit `275b763`, confirmed live via curl (both `/testimonials` and `/partners` resolve 200 through Cloudflare's extension-stripping redirect).
- 2026-07-10: Site-wide em-dash cleanup — found ~90 em-dashes across all 13 HTML pages (prose, image captions, CTA lines, and JS-generated email subject strings) that had accumulated across many prior content-writing sessions despite [RULES.md](RULES.md) already banning them. Louis flagged it directly as reading like an AI tell. Rewrote every instance (sentence splits, commas, colons, or parentheses depending on context) rather than doing a blind character swap. Pushed to `louisklock` at commit `fe979c2`, confirmed 0 remaining matches on the live site (homepage, about, and a blog post spot-checked). Logged a standing feedback memory ([[feedback-no-em-dashes]]) to actively grep for this on future content instead of waiting to be told again.
- 2026-07-13: Added a "Schedule a Meet" top-level nav tab (Residential → Commercial → Schedule a Meet → Resources → Contact, per Louis's requested order) linking to `calendly.com/louisklock` in a new tab, across all 13 pages. Bundled with a PROJECT-MEMORY.md update. Pushed to `louisklock` at commit `24f8459`.
- 2026-07-13: A separate, concurrent Claude session pushed `06cc4c3` directly to `louisklock/main` (Renaissance Global Network + eXp Commercial partner cards on partners.html, sourced via WebSearch since Louis hadn't provided real content yet) while this session's Schedule-a-Meet push was in flight, causing a real diverged-history push rejection. Resolved via `git merge` (not force-push) — partners.html merged cleanly since the two sessions touched different regions of the file; this memory file had a conflict, resolved by keeping both logs. That other session's entry reintroduced 3 em-dashes into partners.html prose (it predates/didn't load the [[feedback-no-em-dashes]] memory) — fixed as part of this merge.
- 2026-07-13: Before pushing the merged partners.html live, showed Louis the two new partner-card descriptions for review (per the approval-gate rule — that content had never been shown to him). He asked for shorter descriptions "true to branding" rather than the longer paraphrased marketing copy. Fetched both companies' actual sites (renaissanceglobalnetwork.com, www.expcommercial.com) and rewrote both card descriptions to closely reflect their own self-description/tagline language, condensed. Confirmed `www.expcommercial.com` is the correct, live URL (resolves the earlier open item about `legacy.expcommercial.com`).
- Open item: swap the two partner-card wordmarks for real logo image files once Louis supplies them (PNG/SVG) — he preferred wordmarks over having Claude guess at unverified logo assets.
- 2026-07-14: Local `main` had diverged from the `louisklock` remote by 5 commits (a separate Claude session had pushed the "Search for Properties Here" hero button — linking to `louisklock.expportal.com` — to `index.html`, `residential.html`, and `commercial.html` directly against `louisklock/main`, which local never merged). Confirmed via `git merge-base --is-ancestor` that this was a clean fast-forward, not a real divergence; merged it in before starting new work so local matches the live site.
- 2026-07-14: Built `residential-search.html` and `commercial-search.html` — full property search UIs (filter bar, listing cards, detail modal, "Recently Sold" section on residential) styled to match the site, backed by placeholder listing data (`assets/js/mock-listings.js`) through a swappable fetch layer (`assets/js/property-data.js`). Neither NWMLS IDX credentials nor a LoopNet advertiser account exist yet — see `PROPERTY-SEARCH-SETUP.md` at repo root for exactly what Louis needs to do to go live with real feeds. Not linked from main nav (matches the "reduce nav clutter" precedent) — the 3 existing "Search for Properties Here" hero buttons remain the intended entry points, but they still point to `louisklock.expportal.com` and have NOT been repointed to these new pages yet; that's a deliberate final step pending Louis's review and approval.
- 2026-07-14: Added anonymous visitor analytics (`assets/js/analytics.js`, session ID in `localStorage`, tracks page views/filter usage/listing clicks) plus an opt-in "Save This Search" lead-capture form (reuses the `resources.html` gated-modal pattern, submits to the existing Formspree endpoint `mdarrrad` and to a not-yet-deployed `/api/save-search`). No backend exists yet for the anonymous side — calls to `/api/track` fail silently and mirror to the visitor's own `localStorage` instead, so the UI is fully demoable locally without a live backend. Real cross-visitor analytics requires Louis to provision a Cloudflare KV namespace + secret and approve a `wrangler deploy` — documented in `PROPERTY-SEARCH-SETUP.md`. Found and fixed a real bug during verification: `analytics.js` originally declared `const LLAnalytics` at top level, which does NOT attach to `window` in a classic script, so `property-search-ui.js`'s `if (window.LLAnalytics)` checks silently never fired — fixed by explicitly assigning `window.LLAnalytics = ...`.
- Open item: the Cloudflare Worker (`src/worker.js`) + KV-backed `/api/track`, `/api/save-search`, `/api/analytics/summary` endpoints and the `admin-analytics.html` dashboard are the next phase (checked in with Louis before building, per his request to review the search UI first) — not built yet as of this entry.
- 2026-07-17: Louis asked to require contact info before releasing the Buyer's/Seller's Roadmap guides — turned out to mean resources.html's existing "Buyer's Guide"/"Seller's Guide" cards (he confirmed via question, not the residential.html roadmap step-sections, which have no download button at all). Added a required phone field to the existing `#guideGateOverlay` gate modal (previously first name + email only), reusing the same Formspree endpoint (`mdarrrad`) and the existing hidden `guide` field for tagging which guide was requested. Also made the Seller's Guide card clickable for the first time — it was previously inert because the click-handler only attached to cards with a real file in `guideFiles`. Per Louis's choice, Seller's Guide now opens the gate and submits the lead (so he starts capturing seller-guide interest), but since no Seller's Guide PDF exists yet, the success message reads "we'll send it to your email" instead of triggering a download. Buyer's Guide flow unchanged/still auto-downloads `assets/louis-lock-buyers-guide.pdf`. Verified both cards open the modal correctly in-browser (did not actually submit test data — that would hit Louis's real Formspree/email). Not pushed live as of this entry.
- 2026-07-17: Louis re-emphasized the guide-tag needed to be unambiguous in the actual Formspree submission, not just inferable. Renamed the hidden tagging field from `name="guide"` to `name="Guide Requested"` (clearer label in Formspree's notification email) and made the `_subject` hidden field dynamic per guide (e.g. "Buyer's Guide Download: Louis Lock" / "Seller's Guide Download: Louis Lock", set in `openGuideGate()`) instead of one static generic subject — so which guide was requested is visible in the inbox subject line itself, not just a form field. Verified both values update correctly via console check in-browser.
